# VMS 志愿活动管理系统 - 集成测试用例

## 测试环境说明

| 项目 | 说明 |
|------|------|
| 后端服务 | 用户服务(8081)、活动服务(8083)、系统服务(8087) |
| 前端服务 | Vue3 + Vite (5173) |
| 数据库 | MySQL 8.0 |

## 测试账号

| 账号 | 密码 | 角色 | 说明 |
|------|------|------|------|
| zyz01 | 123456 | 志愿者 | 正常状态志愿者 |
| zyz02 | 123456 | 志愿者 | 正常状态志愿者 |
| zyz03 | 123456 | 志愿者 | 正常状态志愿者 |
| zz01 | 123456 | 组织负责人 | 已审核通过 |
| zz02 | 123456 | 组织负责人 | 已审核通过 |
| zz03 | 123456 | 组织负责人 | 已禁用/审核拒绝 |
| admin | 123456 | 系统管理员 | 超级管理员 |
| admin01 | 123456 | 系统管理员 | 普通管理员 |

---

## 一、用户认证模块测试

### 1.1 志愿者注册

| 用例编号 | TC-AUTH-001 |
|---------|-------------|
| 用例名称 | 志愿者注册-正常注册 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问注册页面<br>2. 选择"志愿者"角色<br>3. 输入用户名: test_zyz_001<br>4. 输入密码: Test@123<br>5. 输入确认密码: Test@123<br>6. 输入真实姓名: 测试志愿者<br>7. 输入手机号: 13800138001<br>8. 点击注册按钮 |
| 预期结果 | 1. 注册成功，提示"注册成功"<br>2. 自动跳转到登录页面<br>3. 数据库 sys_user 表新增记录，role=0 |
| 接口 | POST /api/auth/register/volunteer |

| 用例编号 | TC-AUTH-002 |
|---------|-------------|
| 用例名称 | 志愿者注册-用户名重复 |
| 前置条件 | 数据库已存在用户名 zyz01 |
| 测试步骤 | 1. 访问注册页面<br>2. 输入已存在的用户名: zyz01<br>3. 其他信息正常填写<br>4. 点击注册按钮 |
| 预期结果 | 1. 注册失败<br>2. 提示"用户名已存在"或类似错误信息 |
| 接口 | POST /api/auth/register/volunteer |

| 用例编号 | TC-AUTH-003 |
|---------|-------------|
| 用例名称 | 志愿者注册-必填项校验 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问注册页面<br>2. 不填写用户名，其他正常填写<br>3. 点击注册按钮 |
| 预期结果 | 1. 注册失败<br>2. 提示"用户名不能为空" |
| 接口 | POST /api/auth/register/volunteer |

| 用例编号 | TC-AUTH-004 |
|---------|-------------|
| 用例名称 | 志愿者注册-密码不一致 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问注册页面<br>2. 输入密码: Test@123<br>3. 输入确认密码: Test@456<br>4. 点击注册按钮 |
| 预期结果 | 1. 注册失败<br>2. 提示"两次密码输入不一致" |
| 接口 | POST /api/auth/register/volunteer |

### 1.2 组织注册

| 用例编号 | TC-AUTH-005 |
|---------|-------------|
| 用例名称 | 组织注册-正常注册 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问注册页面<br>2. 选择"组织负责人"角色<br>3. 填写账号信息：用户名test_org_001、密码Test@123<br>4. 填写组织信息：队伍全称"测试志愿队"、联系人"测试员"、联系电话"13900139001"<br>5. 点击注册按钮 |
| 预期结果 | 1. 注册成功，提示"注册成功，请等待审核"<br>2. 数据库 sys_user 表新增记录，role=1<br>3. 数据库 organization 表新增记录，audit_status=0(待审核) |
| 接口 | POST /api/auth/register/org |

| 用例编号 | TC-AUTH-006 |
|---------|-------------|
| 用例名称 | 组织注册-组织信息缺失 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问注册页面<br>2. 选择"组织负责人"角色<br>3. 只填写账号信息，不填写组织全称<br>4. 点击注册按钮 |
| 预期结果 | 1. 注册失败<br>2. 提示"队伍全称不能为空" |
| 接口 | POST /api/auth/register/org |

### 1.3 用户登录

| 用例编号 | TC-AUTH-007 |
|---------|-------------|
| 用例名称 | 志愿者登录-正常登录 |
| 前置条件 | 存在志愿者账号 zyz01，密码 123456 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入用户名: zyz01<br>3. 输入密码: 123456<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录成功<br>2. 返回 JWT Token<br>3. 自动跳转到志愿者首页<br>4. 显示用户信息（真实姓名、头像等） |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-008 |
|---------|-------------|
| 用例名称 | 管理员登录-正常登录 |
| 前置条件 | 存在管理员账号 admin，密码 123456 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入用户名: admin<br>3. 输入密码: 123456<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录成功<br>2. 返回 JWT Token<br>3. 自动跳转到管理员首页<br>4. 侧边栏显示管理员菜单（组织审核、活动审核、用户管理） |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-009 |
|---------|-------------|
| 用例名称 | 组织登录-审核通过 |
| 前置条件 | 存在组织账号 zz01，审核状态为已通过 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入用户名: zz01<br>3. 输入密码: 123456<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录成功<br>2. 返回 JWT Token<br>3. 自动跳转到组织首页<br>4. 侧边栏显示组织菜单（发布活动、我的活动、报名管理） |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-010 |
|---------|-------------|
| 用例名称 | 组织登录-审核未通过 |
| 前置条件 | 存在组织账号 zz03，审核状态为拒绝 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入用户名: zz03<br>3. 输入密码: 123456<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录失败<br>2. 提示"组织审核未通过，请联系管理员"或显示拒绝原因 |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-011 |
|---------|-------------|
| 用例名称 | 登录-用户不存在 |
| 前置条件 | 无 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入不存在的用户名: notexist<br>3. 输入密码: 123456<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录失败<br>2. 提示"用户名或密码错误" |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-012 |
|---------|-------------|
| 用例名称 | 登录-密码错误 |
| 前置条件 | 存在账号 zyz01 |
| 测试步骤 | 1. 访问登录页面<br>2. 输入用户名: zyz01<br>3. 输入错误密码: wrongpwd<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录失败<br>2. 提示"用户名或密码错误" |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-AUTH-013 |
|---------|-------------|
| 用例名称 | 登录-账号已禁用 |
| 前置条件 | 存在已禁用账号（status=0） |
| 测试步骤 | 1. 访问登录页面<br>2. 输入已禁用的用户名<br>3. 输入正确密码<br>4. 点击登录按钮 |
| 预期结果 | 1. 登录失败<br>2. 提示"账号已被禁用，请联系管理员" |
| 接口 | POST /api/auth/login |

### 1.4 Token验证

| 用例编号 | TC-AUTH-014 |
|---------|-------------|
| 用例名称 | 获取用户信息-正常Token |
| 前置条件 | 已登录，持有有效Token |
| 测试步骤 | 1. 携带Token请求用户信息接口<br>2. GET /api/auth/userinfo |
| 预期结果 | 1. 返回用户信息（userId, username, realName, role等）<br>2. 响应状态码 200 |
| 接口 | GET /api/auth/userinfo |

| 用例编号 | TC-AUTH-015 |
|---------|-------------|
| 用例名称 | 获取用户信息-无Token |
| 前置条件 | 未登录或Token已失效 |
| 测试步骤 | 1. 不携带Token请求用户信息接口<br>2. GET /api/auth/userinfo |
| 预期结果 | 1. 返回错误，状态码 401<br>2. 提示"未登录或登录已过期" |
| 接口 | GET /api/auth/userinfo |

| 用例编号 | TC-AUTH-016 |
|---------|-------------|
| 用例名称 | 获取用户信息-Token过期 |
| 前置条件 | Token已过期 |
| 测试步骤 | 1. 携带过期Token请求用户信息接口 |
| 预期结果 | 1. 返回错误，状态码 401<br>2. 提示"登录已过期，请重新登录" |
| 接口 | GET /api/auth/userinfo |

---

## 二、组织管理模块测试

### 2.1 组织审核（管理员）

| 用例编号 | TC-ORG-001 |
|---------|-------------|
| 用例名称 | 获取组织列表-管理员权限 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. 访问组织审核页面<br>2. GET /api/org/admin/list?page=1&size=10 |
| 预期结果 | 1. 返回组织列表（分页）<br>2. 包含组织名称、审核状态、联系人、联系电话等信息<br>3. 响应状态码 200 |
| 接口 | GET /api/org/admin/list |

| 用例编号 | TC-ORG-002 |
|---------|-------------|
| 用例名称 | 获取组织列表-按审核状态筛选 |
| 前置条件 | 管理员已登录，存在不同审核状态的组织 |
| 测试步骤 | 1. 请求组织列表，筛选条件 auditStatus=0（待审核）<br>2. GET /api/org/admin/list?auditStatus=0 |
| 预期结果 | 1. 返回待审核状态的组织列表<br>2. 所有返回记录的 auditStatus 均为 0 |
| 接口 | GET /api/org/admin/list |

| 用例编号 | TC-ORG-003 |
|---------|-------------|
| 用例名称 | 审核组织-通过 |
| 前置条件 | 管理员已登录，存在待审核组织 |
| 测试步骤 | 1. 选择一个待审核组织<br>2. 点击"通过"按钮<br>3. POST /api/org/admin/audit，参数：orgId, pass=true |
| 预期结果 | 1. 审核成功<br>2. 组织的 audit_status 更新为 1（已通过）<br>3. 记录审核人和审核时间 |
| 接口 | POST /api/org/admin/audit |

| 用例编号 | TC-ORG-004 |
|---------|-------------|
| 用例名称 | 审核组织-拒绝 |
| 前置条件 | 管理员已登录，存在待审核组织 |
| 测试步骤 | 1. 选择一个待审核组织<br>2. 点击"拒绝"按钮<br>3. 输入拒绝原因: "资质不符合要求"<br>4. POST /api/org/admin/audit，参数：orgId, pass=false, rejectReason |
| 预期结果 | 1. 审核成功<br>2. 组织的 audit_status 更新为 2（已拒绝）<br>3. reject_reason 记录拒绝原因 |
| 接口 | POST /api/org/admin/audit |

| 用例编号 | TC-ORG-005 |
|---------|-------------|
| 用例名称 | 获取组织详情 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. GET /api/org/admin/detail/{orgId} |
| 预期结果 | 1. 返回组织详细信息<br>2. 包含队伍名称、单位类型、属地、成立日期、联系人、联系电话、地址、简介等 |
| 接口 | GET /api/org/admin/detail/{orgId} |

| 用例编号 | TC-ORG-006 |
|---------|-------------|
| 用例名称 | 获取组织列表-非管理员权限 |
| 前置条件 | 志愿者账号已登录 |
| 测试步骤 | 1. 志愿者请求组织列表接口<br>2. GET /api/org/admin/list |
| 预期结果 | 1. 返回错误，状态码 403<br>2. 提示"无权限访问" |
| 接口 | GET /api/org/admin/list |

### 2.2 组织信息管理

| 用例编号 | TC-ORG-007 |
|---------|-------------|
| 用例名称 | 组织获取自己的信息 |
| 前置条件 | 组织账号已登录（审核已通过） |
| 测试步骤 | 1. GET /api/org/my |
| 预期结果 | 1. 返回当前登录组织的信息<br>2. 包含队伍名称、联系人、联系电话、地址、简介等 |
| 接口 | GET /api/org/my |

| 用例编号 | TC-ORG-008 |
|---------|-------------|
| 用例名称 | 组织更新信息-正常更新 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 修改联系人电话: 13900000001<br>2. 修改地址: "新地址"<br>3. PUT /api/org/my |
| 预期结果 | 1. 更新成功<br>2. 数据库记录已更新 |
| 接口 | PUT /api/org/my |

| 用例编号 | TC-ORG-009 |
|---------|-------------|
| 用例名称 | 组织更新信息-修改队伍名称 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 尝试修改队伍全称<br>2. PUT /api/org/my，参数包含新的队伍名称 |
| 预期结果 | 1. 根据业务设计，可能允许或禁止修改<br>2. 如禁止，返回错误提示 |
| 接口 | PUT /api/org/my |

---

## 三、活动管理模块测试

### 3.1 发布活动

| 用例编号 | TC-ACT-001 |
|---------|-------------|
| 用例名称 | 发布活动-正常发布 |
| 前置条件 | 组织账号已登录且审核通过 |
| 测试步骤 | 1. 访问发布活动页面<br>2. 填写活动标题: "社区环保志愿活动"<br>3. 选择服务类别: 环境保护<br>4. 选择地区: 北京市-市辖区-朝阳区<br>5. 填写服务对象: 社区居民<br>6. 选择开始时间: 明天<br>7. 选择结束时间: 后天<br>8. 填写活动描述<br>9. 添加岗位: 岗位名"环保宣传员"，计划人数5人<br>10. 点击发布按钮 |
| 预期结果 | 1. 发布成功<br>2. 活动状态为 3（待审核）<br>3. 自动生成项目编号（P开头）<br>4. 岗位信息正确保存 |
| 接口 | POST /api/activity/publish |

| 用例编号 | TC-ACT-002 |
|---------|-------------|
| 用例名称 | 发布活动-多岗位 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 填写活动基本信息<br>2. 添加多个岗位（如：岗位A 3人，岗位B 5人，岗位C 2人）<br>3. 点击发布按钮 |
| 预期结果 | 1. 发布成功<br>2. 所有岗位信息正确保存<br>3. 各岗位的计划人数正确 |
| 接口 | POST /api/activity/publish |

| 用例编号 | TC-ACT-003 |
|---------|-------------|
| 用例名称 | 发布活动-必填项校验 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 不填写活动标题<br>2. 填写其他信息<br>3. 点击发布按钮 |
| 预期结果 | 1. 发布失败<br>2. 提示"活动标题不能为空" |
| 接口 | POST /api/activity/publish |

| 用例编号 | TC-ACT-004 |
|---------|-------------|
| 用例名称 | 发布活动-时间校验 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 填写活动基本信息<br>2. 设置结束时间早于开始时间<br>3. 点击发布按钮 |
| 预期结果 | 1. 发布失败<br>2. 提示"结束时间不能早于开始时间" |
| 接口 | POST /api/activity/publish |

| 用例编号 | TC-ACT-005 |
|---------|-------------|
| 用例名称 | 发布活动-无岗位 |
| 前置条件 | 组织账号已登录 |
| 测试步骤 | 1. 填写活动基本信息<br>2. 不添加任何岗位<br>3. 点击发布按钮 |
| 预期结果 | 1. 发布失败<br>2. 提示"至少需要添加一个岗位" |
| 接口 | POST /api/activity/publish |

| 用例编号 | TC-ACT-006 |
|---------|-------------|
| 用例名称 | 发布活动-志愿者权限校验 |
| 前置条件 | 志愿者账号已登录 |
| 测试步骤 | 1. 志愿者尝试访问发布活动接口<br>2. POST /api/activity/publish |
| 预期结果 | 1. 返回错误，状态码 403<br>2. 提示"无权限发布活动" |
| 接口 | POST /api/activity/publish |

### 3.2 活动审核（管理员）

| 用例编号 | TC-ACT-007 |
|---------|-------------|
| 用例名称 | 获取待审核活动列表 |
| 前置条件 | 管理员已登录，存在待审核活动 |
| 测试步骤 | 1. 访问活动审核页面<br>2. GET /api/activity/admin/pending?status=3 |
| 预期结果 | 1. 返回待审核活动列表<br>2. 包含活动标题、发布组织、状态等信息 |
| 接口 | GET /api/activity/admin/pending |

| 用例编号 | TC-ACT-008 |
|---------|-------------|
| 用例名称 | 审核活动-通过 |
| 前置条件 | 管理员已登录，存在待审核活动 |
| 测试步骤 | 1. 选择一个待审核活动<br>2. 点击"通过"按钮<br>3. POST /api/activity/admin/audit，参数：activityId, pass=true |
| 预期结果 | 1. 审核成功<br>2. 活动状态更新为 0（待启动） |
| 接口 | POST /api/activity/admin/audit |

| 用例编号 | TC-ACT-009 |
|---------|-------------|
| 用例名称 | 审核活动-拒绝 |
| 前置条件 | 管理员已登录，存在待审核活动 |
| 测试步骤 | 1. 选择一个待审核活动<br>2. 点击"拒绝"按钮<br>3. 输入拒绝原因: "活动内容不完整"<br>4. POST /api/activity/admin/audit，参数：activityId, pass=false, rejectReason |
| 预期结果 | 1. 审核成功<br>2. 活动状态更新为 4（已拒绝）<br>3. reject_reason 记录拒绝原因 |
| 接口 | POST /api/activity/admin/audit |

| 用例编号 | TC-ACT-010 |
|---------|-------------|
| 用例名称 | 审核活动-非待审核状态 |
| 前置条件 | 管理员已登录，存在已通过审核的活动 |
| 测试步骤 | 1. 尝试审核一个已通过的活动（状态为0或1）<br>2. POST /api/activity/admin/audit |
| 预期结果 | 1. 审核失败<br>2. 提示"该活动不在待审核状态" |
| 接口 | POST /api/activity/admin/audit |

### 3.3 活动状态管理

| 用例编号 | TC-ACT-011 |
|---------|-------------|
| 用例名称 | 启动活动 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动(status=0) |
| 测试步骤 | 1. 在"我的活动"页面找到待启动的活动<br>2. 点击"启动"按钮<br>3. PUT /api/activity/my/status，参数：activityId, status=1 |
| 预期结果 | 1. 状态更新成功<br>2. 活动状态变为 1（运行中）<br>3. 志愿者可以开始报名 |
| 接口 | PUT /api/activity/my/status |

| 用例编号 | TC-ACT-012 |
|---------|-------------|
| 用例名称 | 结项活动 |
| 前置条件 | 组织账号已登录，存在运行中的活动(status=1) |
| 测试步骤 | 1. 在"我的活动"页面找到运行中的活动<br>2. 点击"结项"按钮<br>3. PUT /api/activity/my/status，参数：activityId, status=2 |
| 预期结果 | 1. 状态更新成功<br>2. 活动状态变为 2（已结项）<br>3. 不再接受报名 |
| 接口 | PUT /api/activity/my/status |

| 用例编号 | TC-ACT-013 |
|---------|-------------|
| 用例名称 | 更新活动状态-非自己的活动 |
| 前置条件 | 组织A已登录，存在组织B发布的活动 |
| 测试步骤 | 1. 组织A尝试修改组织B的活动状态<br>2. PUT /api/activity/my/status |
| 预期结果 | 1. 更新失败<br>2. 提示"无权限操作此活动" |
| 接口 | PUT /api/activity/my/status |

### 3.4 活动更新

| 用例编号 | TC-ACT-014 |
|---------|-------------|
| 用例名称 | 更新活动-待启动状态 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动(status=0) |
| 测试步骤 | 1. 在"我的活动"页面点击"编辑"<br>2. 修改活动标题<br>3. 修改岗位人数<br>4. PUT /api/activity/update |
| 预期结果 | 1. 更新成功<br>2. 活动信息已修改 |
| 接口 | PUT /api/activity/update |

| 用例编号 | TC-ACT-015 |
|---------|-------------|
| 用例名称 | 更新活动-新增岗位 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动 |
| 测试步骤 | 1. 编辑活动，添加新岗位<br>2. PUT /api/activity/update，包含新岗位信息 |
| 预期结果 | 1. 更新成功<br>2. 新岗位已添加到活动 |
| 接口 | PUT /api/activity/update |

| 用例编号 | TC-ACT-016 |
|---------|-------------|
| 用例名称 | 更新活动-删除岗位 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动，活动有多个岗位 |
| 测试步骤 | 1. 编辑活动，删除一个岗位<br>2. PUT /api/activity/update |
| 预期结果 | 1. 更新成功<br>2. 指定岗位已删除<br>3. 如该岗位已有人报名，应根据业务规则处理 |
| 接口 | PUT /api/activity/update |

| 用例编号 | TC-ACT-017 |
|---------|-------------|
| 用例名称 | 更新活动-运行中状态禁止编辑 |
| 前置条件 | 组织账号已登录，存在运行中的活动(status=1) |
| 测试步骤 | 1. 尝试编辑运行中的活动<br>2. PUT /api/activity/update |
| 预期结果 | 1. 更新失败<br>2. 提示"只有待启动状态的活动可以编辑" |
| 接口 | PUT /api/activity/update |

### 3.5 取消活动

| 用例编号 | TC-ACT-018 |
|---------|-------------|
| 用例名称 | 取消活动-待启动状态 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动(status=0)，无报名记录 |
| 测试步骤 | 1. 在"我的活动"页面点击"取消"<br>2. 确认取消操作<br>3. DELETE /api/activity/cancel/{activityId} |
| 预期结果 | 1. 取消成功<br>2. 活动记录被删除<br>3. 相关岗位记录被删除 |
| 接口 | DELETE /api/activity/cancel/{activityId} |

| 用例编号 | TC-ACT-019 |
|---------|-------------|
| 用例名称 | 取消活动-有报名记录 |
| 前置条件 | 组织账号已登录，存在待启动状态的活动，已有志愿者报名 |
| 测试步骤 | 1. 尝试取消已有报名的活动<br>2. DELETE /api/activity/cancel/{activityId} |
| 预期结果 | 1. 根据业务设计：<br>- 可能允许取消但提示会影响已报名志愿者<br>- 或禁止取消，提示"已有志愿者报名，无法取消" |
| 接口 | DELETE /api/activity/cancel/{activityId} |

| 用例编号 | TC-ACT-020 |
|---------|-------------|
| 用例名称 | 取消活动-非自己的活动 |
| 前置条件 | 组织A已登录，存在组织B发布的活动 |
| 测试步骤 | 1. 组织A尝试取消组织B的活动<br>2. DELETE /api/activity/cancel/{activityId} |
| 预期结果 | 1. 取消失败<br>2. 提示"无权限操作此活动" |
| 接口 | DELETE /api/activity/cancel/{activityId} |

### 3.6 活动查询

| 用例编号 | TC-ACT-021 |
|---------|-------------|
| 用例名称 | 活动列表-分页查询 |
| 前置条件 | 系统中存在多条活动记录 |
| 测试步骤 | 1. GET /api/activity/list?page=1&size=10 |
| 预期结果 | 1. 返回第一页数据，每页10条<br>2. 返回总记录数<br>3. 分页信息正确 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-ACT-022 |
|---------|-------------|
| 用例名称 | 活动列表-按状态筛选 |
| 前置条件 | 存在不同状态的活动 |
| 测试步骤 | 1. GET /api/activity/list?status=1（运行中） |
| 预期结果 | 1. 返回状态为运行中的活动列表<br>2. 所有返回记录状态均为1 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-ACT-023 |
|---------|-------------|
| 用例名称 | 活动列表-按服务类别筛选 |
| 前置条件 | 存在不同服务类别的活动 |
| 测试步骤 | 1. GET /api/activity/list?categoryId=education |
| 预期结果 | 1. 返回教育服务类别的活动列表 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-ACT-024 |
|---------|-------------|
| 用例名称 | 活动列表-按地区筛选 |
| 前置条件 | 存在不同地区的活动 |
| 测试步骤 | 1. GET /api/activity/list?regionCode=1101 |
| 预期结果 | 1. 返回北京市辖区的活动列表 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-ACT-025 |
|---------|-------------|
| 用例名称 | 活动列表-关键词搜索 |
| 前置条件 | 存在活动 |
| 测试步骤 | 1. GET /api/activity/list?keyword=环保 |
| 预期结果 | 1. 返回标题或描述中包含"环保"的活动 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-ACT-026 |
|---------|-------------|
| 用例名称 | 获取活动详情 |
| 前置条件 | 存在活动 |
| 测试步骤 | 1. GET /api/activity/detail/{activityId} |
| 预期结果 | 1. 返回活动详细信息<br>2. 包含所有岗位信息<br>3. 包含发布组织信息 |
| 接口 | GET /api/activity/detail/{activityId} |

| 用例编号 | TC-ACT-027 |
|---------|-------------|
| 用例名称 | 组织获取自己的活动列表 |
| 前置条件 | 组织账号已登录，已发布过活动 |
| 测试步骤 | 1. GET /api/activity/my/list |
| 预期结果 | 1. 返回当前组织发布的所有活动<br>2. 包含报名人数统计 |
| 接口 | GET /api/activity/my/list |

---

## 四、志愿者报名与签到模块测试

### 4.1 活动报名

| 用例编号 | TC-REG-001 |
|---------|-------------|
| 用例名称 | 报名活动-正常报名 |
| 前置条件 | 志愿者已登录，存在运行中的活动，岗位有余量 |
| 测试步骤 | 1. 在活动列表点击活动查看详情<br>2. 选择岗位<br>3. 点击"报名"按钮<br>4. POST /api/volunteer/register，参数：activityId, posId |
| 预期结果 | 1. 报名成功<br>2. 报名状态为 0（待审核）<br>3. 岗位已报名人数+1 |
| 接口 | POST /api/volunteer/register |

| 用例编号 | TC-REG-002 |
|---------|-------------|
| 用例名称 | 报名活动-活动未启动 |
| 前置条件 | 志愿者已登录，活动状态为待启动(status=0) |
| 测试步骤 | 1. 尝试报名待启动的活动<br>2. POST /api/volunteer/register |
| 预期结果 | 1. 报名失败<br>2. 提示"活动未开始，无法报名" |
| 接口 | POST /api/volunteer/register |

| 用例编号 | TC-REG-003 |
|---------|-------------|
| 用例名称 | 报名活动-岗位已满 |
| 前置条件 | 志愿者已登录，活动运行中，岗位已满 |
| 测试步骤 | 1. 尝试报名已满员的岗位<br>2. POST /api/volunteer/register |
| 预期结果 | 1. 报名失败<br>2. 提示"该岗位报名人数已满" |
| 接口 | POST /api/volunteer/register |

| 用例编号 | TC-REG-004 |
|---------|-------------|
| 用例名称 | 报名活动-重复报名 |
| 前置条件 | 志愿者已登录，已报名过该活动 |
| 测试步骤 | 1. 尝试再次报名同一活动<br>2. POST /api/volunteer/register |
| 预期结果 | 1. 报名失败<br>2. 提示"您已报名过该活动" |
| 接口 | POST /api/volunteer/register |

| 用例编号 | TC-REG-005 |
|---------|-------------|
| 用例名称 | 取消报名-正常取消 |
| 前置条件 | 志愿者已登录，有待审核的报名记录，未签到 |
| 测试步骤 | 1. 在"我的报名"页面点击"取消报名"<br>2. DELETE /api/volunteer/register/{regId} |
| 预期结果 | 1. 取消成功<br>2. 报名状态变为 3（已取消）<br>3. 岗位已报名人数-1 |
| 接口 | DELETE /api/volunteer/register/{regId} |

| 用例编号 | TC-REG-006 |
|---------|-------------|
| 用例名称 | 取消报名-已签到不允许取消 |
| 前置条件 | 志愿者已登录，有已签到的报名记录 |
| 测试步骤 | 1. 尝试取消已签到的报名<br>2. DELETE /api/volunteer/register/{regId} |
| 预期结果 | 1. 取消失败<br>2. 提示"已有签到记录，无法取消报名" |
| 接口 | DELETE /api/volunteer/register/{regId} |

| 用例编号 | TC-REG-006A |
|---------|-------------|
| 用例名称 | 取消报名-已通过状态岗位人数变化 |
| 前置条件 | 志愿者已登录，有已通过审核的报名记录，未签到 |
| 测试步骤 | 1. 记录当前岗位已报名人数<br>2. 取消报名<br>3. DELETE /api/volunteer/register/{regId}<br>4. 查询岗位人数 |
| 预期结果 | 1. 取消成功<br>2. 报名状态变为 3（已取消）<br>3. 岗位已报名人数-1 |
| 接口 | DELETE /api/volunteer/register/{regId} |

| 用例编号 | TC-REG-006B |
|---------|-------------|
| 用例名称 | 取消报名-非自己的报名 |
| 前置条件 | 志愿者A已登录，存在志愿者B的报名记录 |
| 测试步骤 | 1. 志愿者A尝试取消志愿者B的报名<br>2. DELETE /api/volunteer/register/{regId} |
| 预期结果 | 1. 取消失败<br>2. 提示"只能取消自己的报名" |
| 接口 | DELETE /api/volunteer/register/{regId} |

| 用例编号 | TC-REG-006C |
|---------|-------------|
| 用例名称 | 取消报名-已取消的报名 |
| 前置条件 | 志愿者已登录，有已取消的报名记录 |
| 测试步骤 | 1. 尝试再次取消已取消的报名<br>2. DELETE /api/volunteer/register/{regId} |
| 预期结果 | 1. 取消失败<br>2. 提示"报名已取消" |
| 接口 | DELETE /api/volunteer/register/{regId} |

### 4.2 报名审核（组织端）

| 用例编号 | TC-REG-007 |
|---------|-------------|
| 用例名称 | 获取报名列表 |
| 前置条件 | 组织账号已登录，有志愿者报名 |
| 测试步骤 | 1. GET /api/org/registration/list/{activityId} |
| 预期结果 | 1. 返回该活动的报名列表<br>2. 包含志愿者信息、岗位、状态、签到状态等 |
| 接口 | GET /api/org/registration/list/{activityId} |

| 用例编号 | TC-REG-008 |
|---------|-------------|
| 用例名称 | 审核报名-通过 |
| 前置条件 | 组织账号已登录，有待审核的报名 |
| 测试步骤 | 1. 选择待审核的报名记录<br>2. 点击"通过"<br>3. POST /api/org/registration/audit，参数：regId, pass=true |
| 预期结果 | 1. 审核成功<br>2. 报名状态变为 1（已通过） |
| 接口 | POST /api/org/registration/audit |

| 用例编号 | TC-REG-009 |
|---------|-------------|
| 用例名称 | 审核报名-拒绝 |
| 前置条件 | 组织账号已登录，有待审核的报名 |
| 测试步骤 | 1. 选择待审核的报名记录<br>2. 点击"拒绝"<br>3. POST /api/org/registration/audit，参数：regId, status=2 |
| 预期结果 | 1. 审核成功<br>2. 报名状态变为 2（已拒绝）<br>3. 岗位已报名人数-1 |
| 接口 | POST /api/org/registration/audit |

| 用例编号 | TC-REG-009A |
|---------|-------------|
| 用例名称 | 审核报名-重复审核 |
| 前置条件 | 组织账号已登录，有已处理的报名（已通过/已拒绝） |
| 测试步骤 | 1. 尝试再次审核已处理的报名<br>2. POST /api/org/registration/audit |
| 预期结果 | 1. 审核失败<br>2. 提示"该报名已处理，无法重复审核" |
| 接口 | POST /api/org/registration/audit |

| 用例编号 | TC-REG-009B |
|---------|-------------|
| 用例名称 | 审核报名-无效状态值 |
| 前置条件 | 组织账号已登录，有待审核的报名 |
| 测试步骤 | 1. 尝试审核，传入无效状态值（如status=5）<br>2. POST /api/org/registration/audit |
| 预期结果 | 1. 审核失败<br>2. 提示"审核状态无效" |
| 接口 | POST /api/org/registration/audit |

| 用例编号 | TC-REG-010 |
|---------|-------------|
| 用例名称 | 审核报名-非自己的活动 |
| 前置条件 | 组织A已登录，存在组织B发布的活动的报名 |
| 测试步骤 | 1. 组织A尝试审核组织B的报名<br>2. POST /api/org/registration/audit |
| 预期结果 | 1. 审核失败<br>2. 提示"无权限操作" |
| 接口 | POST /api/org/registration/audit |

### 4.3 签到签退

| 用例编号 | TC-CHK-001 |
|---------|-------------|
| 用例名称 | 签到-正常签到 |
| 前置条件 | 志愿者已登录，有已通过审核的报名，活动运行中，未签到 |
| 测试步骤 | 1. 在"我的报名"页面点击"签到"<br>2. POST /api/volunteer/checkin/{regId} |
| 预期结果 | 1. 签到成功<br>2. 生成签到记录（checkin_log）<br>3. check_type=0 |
| 接口 | POST /api/volunteer/checkin/{regId} |

| 用例编号 | TC-CHK-002 |
|---------|-------------|
| 用例名称 | 签到-重复签到 |
| 前置条件 | 志愿者已登录，已签到过 |
| 测试步骤 | 1. 尝试再次签到<br>2. POST /api/volunteer/checkin/{regId} |
| 预期结果 | 1. 签到失败<br>2. 提示"已签到，请勿重复签到" |
| 接口 | POST /api/volunteer/checkin/{regId} |

| 用例编号 | TC-CHK-003 |
|---------|-------------|
| 用例名称 | 签到-报名未通过 |
| 前置条件 | 志愿者已登录，报名状态为待审核 |
| 测试步骤 | 1. 尝试签到<br>2. POST /api/volunteer/checkin/{regId} |
| 预期结果 | 1. 签到失败<br>2. 提示"报名尚未通过审核" |
| 接口 | POST /api/volunteer/checkin/{regId} |

| 用例编号 | TC-CHK-004 |
|---------|-------------|
| 用例名称 | 签退-正常签退 |
| 前置条件 | 志愿者已登录，已签到，未签退 |
| 测试步骤 | 1. 在"我的报名"页面点击"签退"<br>2. POST /api/volunteer/checkout/{regId} |
| 预期结果 | 1. 签退成功<br>2. 生成签退记录<br>3. check_type=1 |
| 接口 | POST /api/volunteer/checkout/{regId} |

| 用例编号 | TC-CHK-005 |
|---------|-------------|
| 用例名称 | 签退-未签到先签退 |
| 前置条件 | 志愿者已登录，未签到 |
| 测试步骤 | 1. 尝试签退<br>2. POST /api/volunteer/checkout/{regId} |
| 预期结果 | 1. 签退失败<br>2. 提示"请先签到" |
| 接口 | POST /api/volunteer/checkout/{regId} |

| 用例编号 | TC-CHK-006 |
|---------|-------------|
| 用例名称 | 签退-重复签退 |
| 前置条件 | 志愿者已登录，已签退 |
| 测试步骤 | 1. 尝试再次签退<br>2. POST /api/volunteer/checkout/{regId} |
| 预期结果 | 1. 签退失败<br>2. 提示"已签退，请勿重复签退" |
| 接口 | POST /api/volunteer/checkout/{regId} |

| 用例编号 | TC-CHK-006A |
|---------|-------------|
| 用例名称 | 签退-活动已结项 |
| 前置条件 | 志愿者已登录，已签到，活动状态为已结项(status=2) |
| 测试步骤 | 1. 尝试签退<br>2. POST /api/volunteer/checkout/{regId} |
| 预期结果 | 1. 签退成功<br>2. 注：活动运行中(1)或已结项(2)状态都允许签退 |
| 接口 | POST /api/volunteer/checkout/{regId} |

| 用例编号 | TC-CHK-006B |
|---------|-------------|
| 用例名称 | 签到-活动未运行 |
| 前置条件 | 志愿者已登录，报名已通过，活动状态为待启动(status=0) |
| 测试步骤 | 1. 尝试签到<br>2. POST /api/volunteer/checkin/{regId} |
| 预期结果 | 1. 签到失败<br>2. 提示"活动未开始或已结束，无法签到" |
| 接口 | POST /api/volunteer/checkin/{regId} |

| 用例编号 | TC-CHK-006C |
|---------|-------------|
| 用例名称 | 签到-非自己的报名 |
| 前置条件 | 志愿者A已登录，存在志愿者B的报名记录 |
| 测试步骤 | 1. 志愿者A尝试使用志愿者B的报名ID签到<br>2. POST /api/volunteer/checkin/{regId} |
| 预期结果 | 1. 签到失败<br>2. 提示"只能操作自己的报名" |
| 接口 | POST /api/volunteer/checkin/{regId} |

| 用例编号 | TC-CHK-007 |
|---------|-------------|
| 用例名称 | 获取签到状态 |
| 前置条件 | 志愿者已登录，有报名记录 |
| 测试步骤 | 1. GET /api/volunteer/checkin/status/{regId} |
| 预期结果 | 1. 返回签到状态（是否已签到、是否已签退） |
| 接口 | GET /api/volunteer/checkin/status/{regId} |

### 4.4 时长发放

| 用例编号 | TC-HRS-001 |
|---------|-------------|
| 用例名称 | 发放时长-正常发放 |
| 前置条件 | 组织账号已登录，志愿者已签退，未发放时长 |
| 测试步骤 | 1. 在报名管理页面选择已签退的志愿者<br>2. 点击"发放时长"<br>3. 输入时长: 3小时<br>4. POST /api/org/registration/hours，参数：regId, hours=3 |
| 预期结果 | 1. 发放成功<br>2. 生成志愿时长记录（volunteer_record）<br>3. 积分按默认规则计算（10分/小时） |
| 接口 | POST /api/org/registration/hours |

| 用例编号 | TC-HRS-002 |
|---------|-------------|
| 用例名称 | 发放时长-自定义积分 |
| 前置条件 | 组织账号已登录，志愿者已签退 |
| 测试步骤 | 1. 发放时长，指定积分: 50分<br>2. POST /api/org/registration/hours，参数：regId, hours=3, points=50 |
| 预期结果 | 1. 发放成功<br>2. 积分记录为50分 |
| 接口 | POST /api/org/registration/hours |

| 用例编号 | TC-HRS-003 |
|---------|-------------|
| 用例名称 | 发放时长-未签退 |
| 前置条件 | 组织账号已登录，志愿者已签到但未签退 |
| 测试步骤 | 1. 尝试发放时长<br>2. POST /api/org/registration/hours |
| 预期结果 | 1. 发放失败<br>2. 提示"志愿者尚未签退" |
| 接口 | POST /api/org/registration/hours |

| 用例编号 | TC-HRS-004 |
|---------|-------------|
| 用例名称 | 发放时长-重复发放 |
| 前置条件 | 组织账号已登录，志愿者已有时长记录 |
| 测试步骤 | 1. 尝试再次发放时长<br>2. POST /api/org/registration/hours |
| 预期结果 | 1. 发放失败<br>2. 提示"该报名已发放过时长" |
| 接口 | POST /api/org/registration/hours |

### 4.5 志愿者统计与记录

| 用例编号 | TC-STAT-001 |
|---------|-------------|
| 用例名称 | 获取我的报名列表 |
| 前置条件 | 志愿者已登录，有报名记录 |
| 测试步骤 | 1. GET /api/volunteer/my/registrations |
| 预期结果 | 1. 返回当前志愿者的报名列表<br>2. 包含活动信息、岗位、状态、签到状态、时长等 |
| 接口 | GET /api/volunteer/my/registrations |

| 用例编号 | TC-STAT-002 |
|---------|-------------|
| 用例名称 | 获取志愿者统计数据 |
| 前置条件 | 志愿者已登录，有志愿时长记录 |
| 测试步骤 | 1. GET /api/volunteer/my/stats |
| 预期结果 | 1. 返回统计数据：累计时长、累计积分、活动次数、本月时长等 |
| 接口 | GET /api/volunteer/my/stats |

| 用例编号 | TC-STAT-003 |
|---------|-------------|
| 用例名称 | 获取我的时长记录 |
| 前置条件 | 志愿者已登录，有时长记录 |
| 测试步骤 | 1. GET /api/volunteer/my/records |
| 预期结果 | 1. 返回时长记录列表<br>2. 包含活动信息、时长、积分、发放时间等 |
| 接口 | GET /api/volunteer/my/records |

---

## 五、用户管理模块测试

### 5.1 个人信息管理

| 用例编号 | TC-USER-001 |
|---------|-------------|
| 用例名称 | 更新个人信息 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 访问个人中心页面<br>2. 修改真实姓名、手机号<br>3. PUT /api/user/info |
| 预期结果 | 1. 更新成功<br>2. 数据库记录已更新 |
| 接口 | PUT /api/user/info |

| 用例编号 | TC-USER-002 |
|---------|-------------|
| 用例名称 | 修改密码-正确原密码 |
| 前置条件 | 用户已登录，知道当前密码 |
| 测试步骤 | 1. 输入原密码: 123456<br>2. 输入新密码: NewPass@123<br>3. 确认新密码: NewPass@123<br>4. PUT /api/user/password |
| 预期结果 | 1. 修改成功<br>2. 可用新密码登录 |
| 接口 | PUT /api/user/password |

| 用例编号 | TC-USER-003 |
|---------|-------------|
| 用例名称 | 修改密码-错误原密码 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 输入错误原密码<br>2. 输入新密码<br>3. PUT /api/user/password |
| 预期结果 | 1. 修改失败<br>2. 提示"原密码错误" |
| 接口 | PUT /api/user/password |

| 用例编号 | TC-USER-004 |
|---------|-------------|
| 用例名称 | 上传头像-正常上传 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 选择一张图片(JPG/PNG，小于5MB)<br>2. POST /api/file/avatar |
| 预期结果 | 1. 上传成功<br>2. 返回图片URL<br>3. 用户头像已更新 |
| 接口 | POST /api/file/avatar |

| 用例编号 | TC-USER-005 |
|---------|-------------|
| 用例名称 | 上传头像-格式不支持 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 选择一个非图片文件（如.pdf）<br>2. POST /api/file/avatar |
| 预期结果 | 1. 上传失败<br>2. 提示"不支持该文件格式" |
| 接口 | POST /api/file/avatar |

| 用例编号 | TC-USER-006 |
|---------|-------------|
| 用例名称 | 上传头像-文件过大 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 选择一个超过5MB的图片<br>2. POST /api/file/avatar |
| 预期结果 | 1. 上传失败<br>2. 提示"文件大小超过限制" |
| 接口 | POST /api/file/avatar |

### 5.2 管理员用户管理

| 用例编号 | TC-USER-007 |
|---------|-------------|
| 用例名称 | 获取用户列表 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. GET /api/admin/user/list?page=1&size=10 |
| 预期结果 | 1. 返回用户列表（分页）<br>2. 包含用户名、真实姓名、角色、状态等 |
| 接口 | GET /api/admin/user/list |

| 用例编号 | TC-USER-008 |
|---------|-------------|
| 用例名称 | 获取用户列表-按角色筛选 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. GET /api/admin/user/list?role=0（志愿者） |
| 预期结果 | 1. 返回志愿者列表 |
| 接口 | GET /api/admin/user/list |

| 用例编号 | TC-USER-009 |
|---------|-------------|
| 用例名称 | 禁用用户 |
| 前置条件 | 管理员已登录，用户状态正常 |
| 测试步骤 | 1. 选择一个普通用户<br>2. 点击"禁用"<br>3. PUT /api/admin/user/status，参数：userId, status=0 |
| 预期结果 | 1. 禁用成功<br>2. 用户状态变为0（禁用）<br>3. 该用户无法登录 |
| 接口 | PUT /api/admin/user/status |

| 用例编号 | TC-USER-010 |
|---------|-------------|
| 用例名称 | 启用用户 |
| 前置条件 | 管理员已登录，用户状态为禁用 |
| 测试步骤 | 1. 选择一个禁用状态的用户<br>2. 点击"启用"<br>3. PUT /api/admin/user/status，参数：userId, status=1 |
| 预期结果 | 1. 启用成功<br>2. 用户状态变为1（正常）<br>3. 该用户可以正常登录 |
| 接口 | PUT /api/admin/user/status |

| 用例编号 | TC-USER-011 |
|---------|-------------|
| 用例名称 | 禁用管理员账号 |
| 前置条件 | 管理员已登录 |
| 测试步骤 | 1. 尝试禁用另一个管理员账号<br>2. PUT /api/admin/user/status |
| 预期结果 | 1. 操作失败<br>2. 提示"不允许修改管理员账号状态" |
| 接口 | PUT /api/admin/user/status |

| 用例编号 | TC-USER-012 |
|---------|-------------|
| 用例名称 | 用户管理-非管理员权限 |
| 前置条件 | 普通用户已登录 |
| 测试步骤 | 1. 普通用户请求用户列表<br>2. GET /api/admin/user/list |
| 预期结果 | 1. 返回错误，状态码 403<br>2. 提示"无权限访问" |
| 接口 | GET /api/admin/user/list |

---

## 六、系统配置模块测试

### 6.1 字典管理

| 用例编号 | TC-SYS-001 |
|---------|-------------|
| 用例名称 | 获取服务类别字典 |
| 前置条件 | 无需登录 |
| 测试步骤 | 1. GET /api/dict/service_category |
| 预期结果 | 1. 返回服务类别列表<br>2. 包含教育服务、助老服务、环境保护等 |
| 接口 | GET /api/dict/{dictType} |

| 用例编号 | TC-SYS-002 |
|---------|-------------|
| 用例名称 | 获取单位类型字典 |
| 前置条件 | 无需登录 |
| 测试步骤 | 1. GET /api/dict/unit_type |
| 预期结果 | 1. 返回单位类型列表<br>2. 包含企业、学校、社区、政府机关、社会组织等 |
| 接口 | GET /api/dict/{dictType} |

### 6.2 行政区划

| 用例编号 | TC-SYS-003 |
|---------|-------------|
| 用例名称 | 获取省级行政区划 |
| 前置条件 | 无需登录 |
| 测试步骤 | 1. GET /api/system/region/list（不传parentCode） |
| 预期结果 | 1. 返回省级区划列表<br>2. 包含北京市、上海市、江苏省等 |
| 接口 | GET /api/system/region/list |

| 用例编号 | TC-SYS-004 |
|---------|-------------|
| 用例名称 | 获取下级行政区划 |
| 前置条件 | 无需登录 |
| 测试步骤 | 1. GET /api/system/region/list?parentCode=32（江苏省） |
| 预期结果 | 1. 返回江苏省下的市级区划<br>2. 包含南京市、无锡市、苏州市等 |
| 接口 | GET /api/system/region/list |

| 用例编号 | TC-SYS-005 |
|---------|-------------|
| 用例名称 | 获取区级行政区划 |
| 前置条件 | 无需登录 |
| 测试步骤 | 1. GET /api/system/region/list?parentCode=3205（苏州市） |
| 预期结果 | 1. 返回苏州市下的区级区划<br>2. 包含姑苏区、吴中区、相城区等 |
| 接口 | GET /api/system/region/list |

---

## 七、端到端业务流程测试

### 7.1 完整活动生命周期

| 用例编号 | TC-E2E-001 |
|---------|-------------|
| 用例名称 | 完整活动流程-正常流程 |
| 前置条件 | 准备管理员、组织、志愿者账号 |
| 测试步骤 | 1. **组织注册**: 新组织注册，状态待审核<br>2. **管理员审核组织**: 审核通过，组织可登录<br>3. **组织发布活动**: 发布新活动，状态待审核<br>4. **管理员审核活动**: 审核通过，状态待启动<br>5. **组织启动活动**: 状态变为运行中<br>6. **志愿者报名**: 选择岗位报名<br>7. **组织审核报名**: 审核通过<br>8. **志愿者签到**: 活动当天签到<br>9. **志愿者签退**: 活动结束签退<br>10. **组织发放时长**: 发放志愿时长和积分<br>11. **组织结项活动**: 状态变为已结项 |
| 预期结果 | 每个步骤都成功执行，数据状态正确流转 |
| 接口 | 多个接口组合 |

### 7.2 活动拒绝后重新发布

| 用例编号 | TC-E2E-002 |
|---------|-------------|
| 用例名称 | 活动拒绝后重新发布流程 |
| 前置条件 | 组织已登录 |
| 测试步骤 | 1. 组织发布活动A<br>2. 管理员拒绝活动A（填写拒绝原因）<br>3. 组织查看拒绝原因<br>4. 组织修改后重新发布活动B（或编辑原活动） |
| 预期结果 | 1. 活动A状态变为已拒绝<br>2. 组织能看到拒绝原因<br>3. 新活动可正常发布和审核 |
| 接口 | 多个接口组合 |

### 7.3 志愿者取消报名后重新报名

| 用例编号 | TC-E2E-003 |
|---------|-------------|
| 用例名称 | 志愿者取消报名流程 |
| 前置条件 | 志愿者已报名某活动 |
| 测试步骤 | 1. 志愿者取消报名<br>2. 岗位已报名人数-1<br>3. 志愿者重新报名同一活动（选择不同岗位或相同岗位）<br>4. 组织审核通过 |
| 预期结果 | 1. 取消报名成功<br>2. 重新报名成功<br>3. 数据一致性正确 |
| 接口 | 多个接口组合 |

---

## 八、异常与边界测试

### 8.1 参数边界测试

| 用例编号 | TC-BOUND-001 |
|---------|-------------|
| 用例名称 | 分页参数-超出范围 |
| 前置条件 | 活动总数小于100条 |
| 测试步骤 | 1. GET /api/activity/list?page=100&size=10 |
| 预期结果 | 1. 返回空列表<br>2. 不报错 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-BOUND-002 |
|---------|-------------|
| 用例名称 | 分页参数-非法值 |
| 前置条件 | 无 |
| 测试步骤 | 1. GET /api/activity/list?page=-1&size=0 |
| 预期结果 | 1. 系统能正确处理或返回参数错误提示 |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-BOUND-003 |
|---------|-------------|
| 用例名称 | 活动标题-超长字符 |
| 前置条件 | 组织已登录 |
| 测试步骤 | 1. 输入超过100字符的活动标题<br>2. POST /api/activity/publish |
| 预期结果 | 1. 发布失败或自动截断<br>2. 有明确的错误提示 |
| 接口 | POST /api/activity/publish |

### 8.2 并发测试

| 用例编号 | TC-CONC-001 |
|---------|-------------|
| 用例名称 | 报名并发-岗位余量竞争 |
| 前置条件 | 活动运行中，某岗位仅剩1个名额 |
| 测试步骤 | 1. 两个志愿者同时报名同一岗位 |
| 预期结果 | 1. 只有一个报名成功<br>2. 另一个提示"岗位已满"<br>3. 数据一致性正确 |
| 接口 | POST /api/volunteer/register |

### 8.3 数据完整性测试

| 用例编号 | TC-DATA-001 |
|---------|-------------|
| 用例名称 | 删除活动-级联删除验证 |
| 前置条件 | 活动有岗位和报名记录 |
| 测试步骤 | 1. 删除活动<br>2. 检查岗位、报名记录是否正确处理 |
| 预期结果 | 1. 根据业务设计，可能级联删除或禁止删除<br>2. 数据一致性正确 |
| 接口 | DELETE /api/activity/cancel/{activityId} |

---

## 九、安全测试

### 9.1 权限验证测试

| 用例编号 | TC-SEC-001 |
|---------|-------------|
| 用例名称 | 未登录访问受保护接口 |
| 前置条件 | 未登录 |
| 测试步骤 | 1. 不携带Token请求需登录的接口 |
| 预期结果 | 1. 返回401错误<br>2. 提示未登录 |
| 接口 | 任意受保护接口 |

| 用例编号 | TC-SEC-002 |
|---------|-------------|
| 用例名称 | 越权访问-志愿者访问管理接口 |
| 前置条件 | 志愿者已登录 |
| 测试步骤 | 1. 志愿者请求管理员接口<br>2. GET /api/admin/user/list |
| 预期结果 | 1. 返回403错误<br>2. 提示无权限 |
| 接口 | GET /api/admin/user/list |

| 用例编号 | TC-SEC-003 |
|---------|-------------|
| 用例名称 | 越权操作-修改他人数据 |
| 前置条件 | 组织A已登录 |
| 测试步骤 | 1. 组织A尝试修改组织B的活动 |
| 预期结果 | 1. 操作失败<br>2. 提示无权限 |
| 接口 | PUT /api/activity/update |

### 9.2 输入验证测试

| 用例编号 | TC-SEC-004 |
|---------|-------------|
| 用例名称 | SQL注入测试-登录 |
| 前置条件 | 无 |
| 测试步骤 | 1. 用户名输入: admin' OR '1'='1<br>2. 尝试登录 |
| 预期结果 | 1. 登录失败<br>2. 系统正确处理特殊字符，不产生SQL注入 |
| 接口 | POST /api/auth/login |

| 用例编号 | TC-SEC-005 |
|---------|-------------|
| 用例名称 | XSS测试-活动描述 |
| 前置条件 | 组织已登录 |
| 测试步骤 | 1. 活动描述输入: \<script\>alert('xss')\</script\><br>2. 发布活动 |
| 预期结果 | 1. 脚本标签被转义或过滤<br>2. 存储后展示时不会执行脚本 |
| 接口 | POST /api/activity/publish |

---

## 十、性能测试（可选）

### 10.1 接口响应时间

| 用例编号 | TC-PERF-001 |
|---------|-------------|
| 用例名称 | 活动列表响应时间 |
| 测试步骤 | 1. 请求活动列表接口 |
| 预期结果 | 1. 响应时间 < 500ms（正常负载下） |
| 接口 | GET /api/activity/list |

| 用例编号 | TC-PERF-002 |
|---------|-------------|
| 用例名称 | 登录接口响应时间 |
| 测试步骤 | 1. 请求登录接口 |
| 预期结果 | 1. 响应时间 < 300ms |
| 接口 | POST /api/auth/login |

---

## 测试执行记录模板

| 测试日期 | 测试人员 | 用例编号 | 执行结果 | 备注 |
|---------|---------|---------|---------|------|
| | | TC-AUTH-001 | 通过/失败 | |
| | | TC-AUTH-002 | 通过/失败 | |
| ... | ... | ... | ... | ... |

---

*文档版本: v1.0*
*最后更新: 2026-03-10*